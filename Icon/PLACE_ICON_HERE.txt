Place your icon.ico file in this folder.

The file must be named exactly:  icon.ico

It is referenced by:
  - Systems_One_File_Copy_Service.csproj   (embeds icon into the .exe)
  - setup.iss                              (installer and uninstaller icon)

The build and installer will work without it, but the executable and
installer will use the default system icon instead.
